import { LightningElement } from 'lwc';
// typeAttribute0 = primaryLabel  e.g. "P: 100%"  → info variant
// typeAttribute1 = interruptLabel e.g. "I: 100%" → light variant
export default class CapacityCell extends LightningElement {}
