import { LightningElement } from 'lwc';
// Props are injected by primitiveCustomCell as typeAttribute0..N
// typeAttribute0 = statusLabel  (e.g. "All Online")
// typeAttribute1 = statusTime   (e.g. "Since 9:15am")
// typeAttribute2 = dotClass     (e.g. "status-cell__dot status-cell__dot_online")
// typeAttribute3 = labelClass   (e.g. "status-cell__label status-cell__label_online")
export default class StatusCell extends LightningElement {}
